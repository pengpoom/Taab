# Taab 产品定义

## 产品目标

Taab 用一个轻量、原生的 macOS 常驻应用，统一完成窗口切换、窗口浏览和程序坞点击切换。它服务的是“窗口”，而不只是“应用”：同一个应用的多个窗口应该可以独立识别和操作。

## 核心功能

### 1. 横向 Command-Tab 窗口切换器

- 按下 `Command-Tab` 后，在当前鼠标所在显示器中央显示横向缩略图。
- 每张卡片显示实时或最近一次缩略图、应用图标、窗口标题和最小化状态。
- 继续按 `Tab` 向后选择，`Shift-Tab` 向前选择；松开 `Command` 聚焦目标窗口；`Esc` 取消。
- 首版范围为当前用户会话中的普通应用窗口，过滤桌面元素、Taab 自身和不可操作的系统浮层。
- 窗口过多时横向滚动，并始终保证当前选中项可见。

### 2. 每块显示器独立的窗口侧边栏

- 每块显示器分别配置为 `左侧`、`右侧` 或 `关闭`，不使用全局统一方向。
- 侧边栏显示主要位于该显示器上的窗口；点击条目即可恢复并聚焦窗口。
- 显示器接入、拔出或排列变化后，按稳定的显示器标识恢复配置。
- 首版显示当前桌面空间中可用的窗口；跨空间聚合与自动隐藏作为后续增强。

### 3. Windows 风格的程序坞预览与点击

- 鼠标悬停在应用图标上时，显示该应用所有可操作窗口的横向缩略图；点击缩略图即可恢复并聚焦目标窗口。
- 预览使用事件驱动的鼠标监听，只在指针靠近程序坞时实时识别图标，不做周期性全量扫描。
- 点击非前台应用：恢复并聚焦它最近使用的窗口。
- 点击当前前台应用：最小化当前聚焦窗口。
- 应用有多个窗口时，首版只操作最近使用或当前聚焦的一个窗口，不一次性最小化全部窗口。
- 应用尚未运行或没有可操作窗口时，保留程序坞原生行为。
- Taab 无法可靠识别目标时不拦截点击，避免破坏程序坞基本操作。

## 产品原则

- **低延迟**：快捷键出现和选择反馈应当即时；缩略图更新不能阻塞键盘操作。
- **每屏独立**：显示器是一级配置对象，不能只保存一个全局侧边栏方向。
- **安全降级**：权限不足或识别失败时保留 macOS 原生行为。
- **清楚可撤销**：三项能力分别开关，并提供退出和恢复系统默认行为的入口。
- **成熟优先**：优先复用已经验证过的窗口捕获和切换能力，把开发精力放在 Taab 的统一体验上。

## 首版验收标准

1. 获得辅助功能权限后，`Command-Tab` 能打开横向窗口列表、循环选择并聚焦目标。
2. 获得录屏权限后，切换器显示窗口缩略图；权限不足时显示应用图标占位而不崩溃。
3. 两块显示器可以分别设置为左侧和右侧，重启后配置仍然保留。
4. 悬停程序坞应用图标会显示窗口预览；点击前台应用图标会最小化当前窗口，再次点击可恢复并聚焦。
5. 所有功能都可独立关闭；退出 Taab 后系统快捷键和程序坞恢复默认。

## 首版暂不承诺

- Mac App Store 上架与沙盒运行。
- 对所有全屏窗口、舞台管理和不同桌面空间做到完全一致的控制。
- 为已经最小化且从未捕获过的窗口生成实时缩略图。
- 完全复刻 Contexts、AltTab 或 Windows 的外观。

## 已确定与待确认的选择

- 默认快捷键确定使用 `Command-Tab`。
- 侧边栏默认常驻还是自动隐藏。
- 多窗口应用的程序坞点击是否在后续加入窗口选择菜单。
