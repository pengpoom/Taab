# Taab TODO

状态说明：`[ ]` 未开始，`[~]` 进行中，`[x]` 完成。

## M0：干净工程与基础设施

- [x] 归档旧 Glide，建立全新 Taab 工程。
- [x] 确定以 AltTab 为底座、整合 DockMinimize 能力的开发路线。
- [x] 写产品定义、架构与首版验收标准。
- [x] 建立可编译运行的 macOS 工程和测试目标。
- [x] 建立菜单栏入口、设置窗口与启动/退出生命周期。
- [x] 实现权限状态、权限引导和安全降级。
- [x] 建立窗口模型、窗口过滤和基础枚举。

## M1：横向窗口切换器

- [x] 实现快捷键状态机及单元测试。
- [x] 默认接管 `Command-Tab`，并在退出时恢复系统快捷键。
- [x] 建立横向卡片面板与键盘选择。
- [x] 实现窗口恢复和聚焦。
- [x] 接入 ScreenCaptureKit 缩略图和缓存。
- [x] 实现辅助功能/录屏权限不足时的降级。

## M2：每屏独立侧边栏

- [x] 建立显示器稳定标识和 `left / right / off` 持久化。
- [x] 为每块显示器创建独立无激活面板。
- [x] 按窗口主要所在屏幕分组，点击恢复和聚焦。
- [x] 处理分辨率、缩放、排列和显示器热插拔。
- [x] 支持隐藏在屏幕边缘、仅显示图标和保持展开，悬停时自动展开。
- [x] 采用 Contexts 风格的紧凑深色清单，按 Pinned / Other Apps / Full Screen / Desktop N 分组。
- [x] 支持从标题栏添加固定 App、条目右键固定/取消固定，以及未运行 App 的启动与持久化。
- [x] 支持在 Pinned 分组内拖拽调整固定 App 顺序，并跨刷新持久化。
- [x] 面板按内容自适应高度，并修复收回时窗口宽度与内部图标布局不同步。
- [x] 使用无淡入淡出的抽屉动画，并区分蓝色当前焦点与浅色鼠标悬停反馈。

## M3：程序坞点击切换

- [x] 实现纯函数式 Dock 决策表并覆盖测试。
- [x] 识别被点击的 Dock 应用图标。
- [x] 悬停应用图标时显示多窗口缩略图，点击卡片恢复并聚焦。
- [x] 程序坞悬停预览支持“全部应用 / 仅多窗口应用 / 从不显示”三档策略。
- [x] 使用事件驱动的实时命中检测，支持程序坞底部、左侧、右侧和多显示器定位。
- [x] 非前台应用：恢复并聚焦最近窗口。
- [x] 前台应用：最小化当前聚焦窗口。
- [x] 无窗口、无权限和识别失败时保留系统行为。
- [ ] 验证 Dock 自动隐藏、左右/底部位置和多显示器。

## M4：发布准备

- [x] 添加独立的 Taab 设置页，集中管理自定义功能。
- [ ] 设置登录时启动、更新机制和崩溃恢复策略。
- [ ] 完成双屏/全屏/空间/舞台管理测试矩阵。
- [ ] 生成签名、公证的 `.app` 安装包。

## 当前验收记录

- [x] Debug 构建成功，`Taab.app` 可启动并保持运行。
- [x] 设置界面实际显示主快捷键为 `⌘ + ⇥`，默认外观为横向缩略图。
- [x] 本地版不显示试用、升级入口或 Pro 徽标。
- [x] 侧边栏、Dock 点击决策、Command-Tab 接管均有定向测试覆盖。
- [ ] 在两块真实显示器上完成左右侧边栏与 Dock 点击的人工交互矩阵。
